<h1>Add a Movie to Your <%= @list.name %> List</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit 'Add Movie', class: 'btn btn-white' %>
<% end %>